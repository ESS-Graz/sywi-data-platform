# AUTO-GENERATED - DO NOT EDIT
# Production Docker Compose configuration
# Usage: docker compose -f docker-compose.prod.yaml up --build

services:
  postgresql:
    image: dhi.io/postgres:18-alpine3.22
    environment:
      POSTGRES_USER: dagster_user
      POSTGRES_PASSWORD: dagster_password
      POSTGRES_DB: dagster_db
    volumes:
    - dagster_pgdata_pg18:/var/lib/postgresql
    - ./init-db.sh:/docker-entrypoint-initdb.d/init-db.sh:ro
    networks:
    - sywi-dagster
    healthcheck:
      test:
      - CMD-SHELL
      - pg_isready -U dagster_user -d dagster_db
      interval: 5s
      timeout: 5s
      retries: 5
  dagster_daemon:
    build:
      context: .
      dockerfile: Dockerfile.dagster
    command: dagster-daemon run
    environment:
      DAGSTER_HOME: /opt/dagster/dagster_home
      SYWI_RAW_DATA_DIR: /opt/dagster/raw_data
    env_file: .env
    volumes:
    - ./workspace.yaml:/opt/dagster/app/workspace.yaml
    - ./dagster.yaml:/opt/dagster/dagster_home/dagster.yaml
    - ./data:/opt/dagster/app/data
    - ./data/raw:/opt/dagster/raw_data:ro
    depends_on:
      postgresql:
        condition: service_healthy
      project_hackernews:
        condition: service_started
      project_ikariam:
        condition: service_started
      project_patents:
        condition: service_started
    networks:
    - sywi-dagster
    restart: unless-stopped
  dagster_webserver:
    build:
      context: .
      dockerfile: Dockerfile.dagster
    command: dagster-webserver -h 0.0.0.0 -p 3000 -w workspace.yaml --path-prefix
      /dagster
    environment:
      DAGSTER_HOME: /opt/dagster/dagster_home
      DAGSTER_UI_PATH_PREFIX: /dagster
      SYWI_RAW_DATA_DIR: /opt/dagster/raw_data
    env_file: .env
    volumes:
    - ./workspace.yaml:/opt/dagster/app/workspace.yaml
    - ./dagster.yaml:/opt/dagster/dagster_home/dagster.yaml
    - ./data:/opt/dagster/app/data
    - ./data/raw:/opt/dagster/raw_data:ro
    ports:
    - 3000:3000
    depends_on:
      postgresql:
        condition: service_healthy
      dagster_daemon:
        condition: service_started
      project_hackernews:
        condition: service_started
      project_ikariam:
        condition: service_started
      project_patents:
        condition: service_started
    networks:
    - sywi-dagster
    restart: unless-stopped
  project_hackernews:
    build:
      context: .
      dockerfile: hackernews/Dockerfile
    env_file: .env
    environment:
      SYWI_RAW_DATA_DIR: /opt/dagster/raw_data
    volumes:
    - ./data/raw:/opt/dagster/raw_data:ro
    expose:
    - '4000'
    networks:
    - sywi-dagster
    restart: unless-stopped
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    depends_on:
      postgresql:
        condition: service_healthy
  project_ikariam:
    build:
      context: .
      dockerfile: ikariam/Dockerfile
    env_file: .env
    environment:
      SYWI_RAW_DATA_DIR: /opt/dagster/raw_data
    volumes:
    - ./data/raw:/opt/dagster/raw_data:ro
    expose:
    - '4000'
    networks:
    - sywi-dagster
    restart: unless-stopped
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    depends_on:
      postgresql:
        condition: service_healthy
  project_patents:
    build:
      context: .
      dockerfile: patents/Dockerfile
    env_file: .env
    environment:
      SYWI_RAW_DATA_DIR: /opt/dagster/raw_data
    volumes:
    - ./data/raw:/opt/dagster/raw_data:ro
    expose:
    - '4000'
    networks:
    - sywi-dagster
    restart: unless-stopped
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    depends_on:
      postgresql:
        condition: service_healthy
networks:
  sywi-dagster:
    name: sywi-dagster
    driver: bridge
volumes:
  dagster_pgdata_pg18: null
